# =============================================================================
# Backend Environment Variables Template
# Copy to .env (local dev) or set in Render Dashboard (production)
# NEVER commit the actual .env file — only this .env.example
# =============================================================================

# ── Server ────────────────────────────────────────────────────────────────────
# Supabase pooler (recommended for Render production):
#   Session mode:  ...pooler.supabase.com:5432/postgres  (pool cap ~15 clients)
#   Transaction:   ...pooler.supabase.com:6543/postgres  (higher concurrency)
# Do NOT point local `cargo run` at production DATABASE_URL during Render deploys.
DATABASE_URL=postgresql://postgres.[PROJECT-REF]:[PASSWORD]@aws-0-[REGION].pooler.supabase.com:5432/postgres
# Optional override — production defaults to max 5 connections (Supabase Session limit)
# DATABASE_POOL_SIZE=5
BACKEND_HOST=0.0.0.0
BACKEND_PORT=8080
PORT=8080
RUST_LOG=info
ENVIRONMENT=production

# ── JWT ───────────────────────────────────────────────────────────────────────
# Minimum 32 characters each — generate with: openssl rand -base64 48
JWT_SECRET=your-super-secret-jwt-key-minimum-32-characters
JWT_REFRESH_SECRET=your-super-secret-refresh-key-minimum-32-characters
JWT_EXPIRY_HOURS=24
JWT_REFRESH_EXPIRY_DAYS=30

# ── TOTP / 2FA (ADMIN Google Authenticator) ─────────────────────────────────
# 32-byte key, base64-encoded — generate with: openssl rand -base64 32
# Paste the full output including any leading + or trailing = characters.
TOTP_ENCRYPTION_KEY=your-base64-32-byte-encryption-key

# ── CORS (must exactly match the browser's Origin header) ────────────────────
FRONTEND_URL=https://www.mvrconsultants.org
ALLOWED_ORIGINS=https://www.mvrconsultants.org

# ── Redis — required in production (admin logout token revocation) ───────────
# Local Docker: auto-set via docker-compose (redis service).
# Render production: provisioned automatically via render.yaml (mvr-redis).
# Leave unset only for bare-metal local dev without Docker.
REDIS_PASSWORD=your-redis-password
REDIS_URL=redis://:your-redis-password@redis:6379

# ── Cloudinary (admin image uploads — signed API) ───────────────────────────
# Required: cloud name, API key, and API secret. The backend signs uploads via
# POST /api/admin/cloudinary/sign — no upload preset is needed.
CLOUDINARY_CLOUD_NAME=your-cloud-name
CLOUDINARY_API_KEY=your-api-key
CLOUDINARY_API_SECRET=your-api-secret    # NEVER expose this; server-side only
# CLOUDINARY_UPLOAD_PRESET is unused (signed uploads do not require a preset)

# ── Email — Resend (https://resend.com) ───────────────────────────────────────
# Required for contact form email notifications (Render free tier cannot use SMTP).
#
# Setup:
#   1. Create account at https://resend.com
#   2. Add domain mvrconsultants.org → copy SPF/DKIM DNS records into Hostinger hPanel
#   3. Create API key → set RESEND_API_KEY below and on Render (mvr-backend service)
#   4. EMAIL_FROM must use the verified domain (noreply@ or notifications@)
#   5. ADMIN_EMAIL is where lead alerts are delivered (your Hostinger inbox)
#
RESEND_API_KEY=re_your_api_key
EMAIL_FROM=noreply@mvrconsultants.org
EMAIL_FROM_NAME=MVR Consultants
ADMIN_EMAIL=guntur@mvrconsultants.org
ADMIN_EMAIL_GUNTUR=guntur@mvrconsultants.org
